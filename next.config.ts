import type {NextConfig} from 'next';

import './src/env'; // <--- Voeg deze import toe om validatie te triggeren bij build/start

const nextConfig: NextConfig = {
  // Je bestaande config
};

export default nextConfig;
